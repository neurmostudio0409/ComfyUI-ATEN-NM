# ATEN AIVoice API 設定
# 複製本檔為 config/.env 後填入（套件根目錄的 .env 也會被讀取）
ATEN_API_TOKEN=<paste-your-token-here>

# API Server 位置（預設如下，如需使用其他 API Server 請取消註解並修改）
# ATEN_API_URL=https://www.aivoice.com.tw/atzone
